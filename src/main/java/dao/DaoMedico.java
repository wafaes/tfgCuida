package dao;

import java.util.List;

import jakarta.persistence.EntityManager;
import model.Medico;


public class DaoMedico extends BaseJPADao{

	/*public List<Medico> getMedicosById(Integer idmedico){
		EntityManager em = getEntityManager();
		return em.find(Meidoc.class, idcategoria).getPlatos();
	}
	
	public Plato getPlatoById(long idPlato) {
		EntityManager em = getEntityManager();
		return em.find(Plato.class, idPlato);
	}*/
	
}
